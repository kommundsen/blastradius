{
  "name": "blastradius-repo",
  "private": true,
  "description": "Dev tooling only — ui/ itself has no build step and no runtime dependencies (ADR-0011).",
  "scripts": {
    "test:layout": "node --test ui/tests/determinism.test.mjs",
    "test:webkit": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
